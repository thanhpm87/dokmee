@model Web.ViewModels.Home.IndexModel

@{
    ViewBag.Title = "Home Page";
}
<section>
    <div class="row">
        <div class="col-md-12">
            <h4>Cabinets</h4>
            <table class="table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Name</th>
                    </tr>
                </thead>

                <tbody>
                    @foreach (var item in Model.Cabinets)
                    {
                        <tr>
                            <td>@item.ID</td>
                            <td>
                                <a>
                                    @item.Name
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</section>
